#include <stdio.h>

#include "lexer/lexer.h"

#define SUCCESS 0
#define FAILURE 1

int parser(char* fileContents) {
    //if(lex(fileContents) == SUCCESS)
    //    return SUCCESS;
    //return FAILURE;
    return SUCCESS;
}